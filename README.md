# AWS Lambda Python Package Versions

Creates a temporary lambda function that returns the current version of the packages installed in the lambda environments.


### Usage
```shell
git fetch ./get_pkgs.sh

```